# Minimal.Log - 极简主义静态博客系统

这是一个基于 **React 18** 和 **Vite** 构建的超轻量级、零负担博客系统。

## 🏗️ 核心特性
- **纯粹的 ESM**: 运行时通过 `esm.sh` 加载依赖，构建产物极小。
- **自动化部署**: 完美适配 GitHub Actions，推送到 `main` 分支即可自动发布。
- **SEO 友好**: 采用 Hash 路由确保在 GitHub Pages 这种不支持 SPA 路径的平台上也能正常刷新。
- **资产看板**: 实时持仓看板，集成腾讯行情 API。

## 🚀 部署指引

1. **GitHub 仓库设置**:
   - 推送代码到 GitHub。
   - 进入 `Settings -> Actions -> General`，勾选 `Read and write permissions`。
   - 进入 `Settings -> Pages`，将 `Source` 设置为 `GitHub Actions`。

2. **内容更新**:
   - 文章存放在 `public/content/` 目录下。
   - 新增文章后，需在 `data/posts.ts` 的 `POST_IDS` 数组中手动注册 ID 以显示。

## 📝 维护手册

### ✍️ 如何添加新文章

1. **创建 Markdown 文件**：
   在 `public/content/` 目录下创建新的 `.md` 文件，例如 `my-new-post.md`。

2. **添加文章元信息**（Frontmatter）：
   ```markdown
   ---
   title: "你的文章标题"
   date: "2024-03-20"
   tags: ["标签1", "标签2"]
   description: "文章简介（可选）"
   ---

   # 文章正文开始

   这里是你的文章内容...
   ```

3. **注册文章 ID**：
   打开 `data/posts.ts`，在 `POST_IDS` 数组中添加文件名（不含 `.md` 扩展名）：
   ```typescript
   export const POST_IDS: string[] = [
     'my-new-post',  // 新增这一行
     'react-18-guide',
     'tailwind-tips',
     'welcome-post'
   ];
   ```

4. **提交并推送**：
   ```bash
   git add .
   git commit -m "Add new post: my-new-post"
   git push
   ```
   GitHub Actions 会自动构建并部署到 GitHub Pages。

### 💼 如何修改持仓配置

1. **编辑持仓文件**：
   打开 `data/portfolio.ts`，修改 `MY_PORTFOLIO` 数组：
   ```typescript
   export const MY_PORTFOLIO: StockHolding[] = [
     { symbol: '0700.HK', name: '腾讯控股', weight: 0.20 },
     { symbol: '600519.SS', name: '贵州茅台', weight: 0.15 },
     // 添加新的持仓
     { symbol: '000001.SZ', name: '平安银行', weight: 0.10 }
   ];
   ```

2. **股票代码格式说明**：
   - **港股**：`0700.HK`（腾讯控股）
   - **沪市 A 股**：`600519.SS`（贵州茅台）
   - **深市 A 股**：`000651.SZ`（格力电器）
   - **沪市 B 股**：`900905.SS`（老凤祥 B）
   - **深市 B 股**：`200596.SZ`（古井贡 B）

3. **权重说明**：
   - `weight` 表示该资产在投资组合中的占比（0-1 之间的小数）
   - 例如：`0.20` 表示 20%，`0.15` 表示 15%
   - 所有权重之和建议为 1.0（100%），但不强制

4. **提交并推送**：
   ```bash
   git add data/portfolio.ts
   git commit -m "Update portfolio holdings"
   git push
   ```

### 📄 其他维护操作

- **关于页面**：直接修改 `public/about.md`。
- **动画与样式**：全局进场动画在 `index.html` 的 `<style>` 标签中定义。
- **SEO 配置**：sitemap 会在构建时自动生成（`scripts/generate-sitemap.js`）。

## 🛠️ 本地开发
```bash
npm install
npm run dev
```

## ⚖️ 协议
MIT - 专注于内容，忘掉复杂。