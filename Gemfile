source 'https://rubygems.org'

# GitHub Pages dependency versions (matches the Pages build environment)
gem 'github-pages', group: :jekyll_plugins

# Plugins
group :jekyll_plugins do
  gem 'jekyll-paginate'
end
